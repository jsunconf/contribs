var assert = require('assert')
  , tests
  , controller = geddy.controller.create('Contribs');

tests = {
  'test stub, replace with your own passing test': function () {
    assert.equal(true, true);
  }

};

module.exports = tests;
